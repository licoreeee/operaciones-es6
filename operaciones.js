/* Ricardo Alán Gutiérrez Garcés - 247398
   Abel Eduardo Sánchez Guerrero - 245473
   Diego Valenzuela Parra - 247700
   Victoria Vega Bernal - 247804 */

// Exportamos la función suma.
export function suma(a, b) {
    return a + b;
}

//Exportamos la función resta.
export function resta(a, b) {
    return a - b;
}

//Exportamos la función multiplicación.
export function multiplicacion(a,b){
    return a * b;
}

//Creamos la función división.
function division(a, b) {
    return a / b;
}

// Creamos la constante pi.
const pi = 3.1416;

// Exportamos las dos entidades.
const pi = 3.1416;

export {division, pi}

// Exportamos la función.
export function modulo(a, b) {
    return a % b;
}